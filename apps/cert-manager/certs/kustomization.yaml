apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: certs
  namespace: flux-system
spec:
  interval: 5m
  path: "./apps/cert-manager/certs"
  prune: true
  dependsOn:
    - name: issuers
  sourceRef:
    kind: GitRepository
    name: flux-system